# dram-patterns

From the repository root:

```bash
./sweep-se.sh
python3 scripts/plot_results.py dram-patterns
```

See [experiment methods](../README.md) for variables, measurements, interpretation, and DRAM mapping caveats. Outputs go to `results/dram-patterns/`, which is excluded from Git; generated plots go to `assets/plots/`.
